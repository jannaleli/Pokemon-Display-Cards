# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'ScootRenewed' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!
  # Pods for pokemon
  pod 'Alamofire'
  pod 'AlamofireImage'
  pod 'SwiftyJSON'
  # Pods for ScootRenewed

  target 'ScootRenewedTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'ScootRenewedUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
